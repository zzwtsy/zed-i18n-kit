{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/zed-i18n-kit/golden-corpus-sample-v2.schema.json",
  "title": "Zed UI text golden sample v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "path",
    "source_span",
    "anchor",
    "scope",
    "subject_kind",
    "sink_symbol",
    "text_slot",
    "sink_kind",
    "features",
    "ownership",
    "expected_presence",
    "expected_disposition",
    "review_state",
    "rationale"
  ],
  "properties": {
    "id": { "type": "string", "pattern": "^zed-[0-9a-f]{7}-[0-9]{4}$" },
    "path": { "type": "string", "pattern": "^crates/.+\\.rs$" },
    "source_span": {
      "type": "object",
      "additionalProperties": false,
      "required": ["start_byte", "end_byte"],
      "properties": {
        "start_byte": { "type": "integer", "minimum": 0 },
        "end_byte": { "type": "integer", "minimum": 1 }
      }
    },
    "anchor": { "type": "string", "minLength": 1 },
    "scope": {
      "enum": ["production", "test", "example", "component_preview"]
    },
    "subject_kind": {
      "enum": ["sink_slot", "expression_origin", "scope_exclusion"]
    },
    "sink_symbol": { "type": ["string", "null"] },
    "text_slot": { "type": ["string", "null"] },
    "sink_kind": {
      "enum": [
        "visible_text",
        "tooltip",
        "accessibility",
        "toast",
        "prompt",
        "none",
        "unknown"
      ]
    },
    "features": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "enum": [
          "direct_literal",
          "builder_argument",
          "child_argument",
          "local_variable",
          "match_expression",
          "if_expression",
          "format_template",
          "concatenation",
          "dynamic_external",
          "element_id",
          "log_or_diagnostic",
          "path_url_or_command",
          "toast",
          "prompt",
          "accessibility",
          "test_scope",
          "preview_scope",
          "example_scope"
        ]
      }
    },
    "ownership": {
      "enum": [
        "product",
        "mixed",
        "user",
        "protocol",
        "developer",
        "identity",
        "unknown"
      ]
    },
    "expected_presence": { "enum": ["candidate", "not_candidate"] },
    "expected_disposition": {
      "enum": ["confirmed", "review_required", "excluded"]
    },
    "review_state": {
      "enum": ["single_review", "independently_reviewed", "disputed"]
    },
    "rationale": { "type": "string", "minLength": 1 }
  }
}
